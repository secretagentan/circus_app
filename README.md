# Working in Rails

# Setup

Download dependencies

```
$ bundle install
```

Setup database

```
$ rails db:create
$ rails db:migrate
$ rails db:seed
```

# Adding dependencies

```
# Gemfile
gem 'pry-rails'
```
